import styled from 'styled-components';
// TODO: instead of margin-top use something else
export const SideNavDiv = styled.div`
     {
        width: 200px;
        height: 80vh;
        background: red;
        color: white;
        margin-top: 45px;
    }
`;
